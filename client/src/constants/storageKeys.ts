/**
 * Storage Keys Constants
 * Các key constants cho localStorage/sessionStorage
 */

export const STORAGE_KEYS = {
  TOKEN: 'token',
  USERNAME: 'username',
  ROLE: 'role',
  CART_ITEMS: 'cartItems',
  USER_DATA: 'userData',
} as const;
